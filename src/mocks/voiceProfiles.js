// Backward-compatible import for older call sites. Production code imports the
// first-party persona catalog from constants/voiceProfiles.
export { voiceProfiles } from '../constants/voiceProfiles';
